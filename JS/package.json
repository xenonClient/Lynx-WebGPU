{
  "name": "lynx-webgpu-js",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Lynx-WebGPU JS shim — 런타임 의존성 없음. TypeScript는 검사·선언 생성 전용",
  "scripts": {
    "test": "NODE_OPTIONS=--expose-gc node --test 'tests/*.test.mjs'",
    "typecheck": "tsc --noEmit",
    "types": "tsc"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
